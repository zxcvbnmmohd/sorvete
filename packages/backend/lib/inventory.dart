// Re-export shim for inventory_client. Use a prefixed import so type names
// (Client, plus generated model classes) don't collide with other services.
//
// Usage:
//   import 'package:sorvete_backend/inventory.dart' as inventory;
export 'package:inventory_client/inventory_client.dart';
