services:
  # Development services
  postgres:
    image: pgvector/pgvector:pg18
    ports:
      - "8090:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: ads
      POSTGRES_PASSWORD: "${SERVICE_DB_PASSWORD:-devpass}"
    volumes:
      - ads_data:/var/lib/postgresql

  # Test services
  postgres_test:
    image: pgvector/pgvector:pg18
    ports:
      - "9090:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: ads_test
      POSTGRES_PASSWORD: "${SERVERPOD_PASSWORD_database:?set in services/<svc>/server/.env (local) or CI secret}"
    volumes:
      - ads_test_data:/var/lib/postgresql

volumes:
  ads_data:
  ads_test_data:
