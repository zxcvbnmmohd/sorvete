services:
  # Development services
  postgres:
    image: pgvector/pgvector:pg18
    ports:
      - "8090:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: inventory
      POSTGRES_PASSWORD: "${SERVICE_DB_PASSWORD:-devpass}"
    volumes:
      - inventory_data:/var/lib/postgresql

  # Test services
  postgres_test:
    image: pgvector/pgvector:pg18
    ports:
      - "9090:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: inventory_test
      POSTGRES_PASSWORD: "${SERVERPOD_PASSWORD_database:?set in services/<svc>/server/.env (local) or CI secret}"
    volumes:
      - inventory_test_data:/var/lib/postgresql

volumes:
  inventory_data:
  inventory_test_data:
