services:
  # Development services
  postgres:
    image: pgvector/pgvector:pg18
    ports:
      - "8090:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: support
      POSTGRES_PASSWORD: "${SERVICE_DB_PASSWORD:-devpass}"
    volumes:
      - support_data:/var/lib/postgresql

  # Test services
  postgres_test:
    image: pgvector/pgvector:pg18
    ports:
      - "9090:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: support_test
      POSTGRES_PASSWORD: "${SERVERPOD_PASSWORD_database:?set in services/<svc>/server/.env (local) or CI secret}"
    volumes:
      - support_test_data:/var/lib/postgresql

volumes:
  support_data:
  support_test_data:
